# Supabase (Stage 2 – application form backend)
# Get these from Supabase: Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: Staging table for lookup-by-email prefill (default: staging)
# Lookup matches email against: Email 1–6, Owner 2 Email 1–5. If table not found, we also try opposite case (Staging/staging).
# SUPABASE_STAGING_TABLE=staging

# Optional: for one-time setup script (npm run setup:supabase) to create tables
# Get from Supabase: Project Settings → Database → Connection string → URI (Session)
# SUPABASE_DB_URL=postgresql://postgres.[ref]:[PASSWORD]@...supabase.com:5432/postgres

# Resend (application emails: to merchant + to subs@onedaycap.com)
# Get from Resend: https://resend.com/api-keys. Verify domain onedaycap.com in Resend → Domains.
RESEND_API_KEY=re_xxxxxxxxxxxx
# Optional: from address (default subs@onedaycap.com). Must be a verified domain in Resend.
# RESEND_FROM_EMAIL=subs@onedaycap.com
