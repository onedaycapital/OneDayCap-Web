# Supabase – use Merchant DB project only (not WatermarkFile). See docs/SUPABASE_PROJECTS.md
# Get these from Merchant DB: Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: Staging table for lookup-by-email prefill (default: staging)
# Lookup matches email against: Email 1–6, Owner 2 Email 1–5. If table not found, we also try opposite case (Staging/staging).
# SUPABASE_STAGING_TABLE=staging

# Optional: Staging table primary key column name (default: id). Set if your table uses a different PK name, e.g. "Primary Key" for uuid.
# SUPABASE_STAGING_ID_COLUMN=Primary Key

# Optional: Pre-staging table for admin Upload merchant DB (default: pre_staging)
# Use Pre_Staging (capital P) if your large uploads are in that table; set this so count, process, and upload all use it.
# SUPABASE_PRE_STAGING_TABLE=Pre_Staging

# Optional: Pre-staging table primary key column name (default: id). Set if your table uses a different PK name, e.g. uuid PK shown as "Primary Key" in Supabase.
# SUPABASE_PRE_STAGING_ID_COLUMN=Primary Key

# Optional: Table name for the *other* pre-staging table (default: Pre_Staging when main is pre_staging). Set if the API expects a different name (e.g. pre_staging if PostgREST only exposes one).
# SUPABASE_OTHER_PRE_STAGING_TABLE=Pre_Staging

# Optional: Primary key column for the other pre-staging table. Default: id. Set to your table's PK, e.g. "Primary Key" (with space) or Id.
# SUPABASE_OTHER_PRE_STAGING_ID_COLUMN="Primary Key"

# Optional: for one-time setup script (npm run setup:supabase) to create tables
# Get from Supabase: Project Settings → Database → Connection string → URI (Session)
# SUPABASE_DB_URL=postgresql://postgres.[ref]:[PASSWORD]@...supabase.com:5432/postgres

# Resend (application emails: to merchant + to subs@onedaycap.com)
# Get from Resend: https://resend.com/api-keys. Verify domain onedaycap.com in Resend → Domains.
RESEND_API_KEY=re_xxxxxxxxxxxx
# Optional: from address (default subs@onedaycap.com). Must be a verified domain in Resend.
# RESEND_FROM_EMAIL=subs@onedaycap.com

# Admin (Phase 1). Password required to access /admin. Set a strong value in production.
# ADMIN_PASSWORD=your-admin-password

# Optional: for admin Upload contract/guidelines – PDF extraction via OpenAI (gpt-4o-mini).
# OPENAI_API_KEY=sk-...

# Cron (abandonment nudges, 15-day follow-up, funnel digest). Set a secret and use it in Vercel Cron env.
# CRON_SECRET=your-secret-here

# Optional: base URL for resume/unsubscribe links in emails (default https://www.onedaycap.com or VERCEL_URL).
# NEXT_PUBLIC_APP_URL=https://www.onedaycap.com
