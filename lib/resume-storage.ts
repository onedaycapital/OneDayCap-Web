/** SessionStorage keys used by /apply/resume to pass email and step to the apply form. */
export const RESUME_EMAIL_KEY = "resume_email";
export const RESUME_STEP_KEY = "resume_step";
/** Set by /apply/documents?t=... when resuming to step 5 (submit documents) so form has savedApplicationId. */
export const RESUME_APPLICATION_ID_KEY = "resume_application_id";
